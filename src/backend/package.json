{
  "name": "streamfile-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "bun --watch server.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build/bundle-backend.ts",
    "test": "bun test tests/"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
